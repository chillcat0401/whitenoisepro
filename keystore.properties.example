# Copy to keystore.properties and replace every placeholder.
# The real file and keystore are ignored by version control.
storeFile=/absolute/path/to/upload-keystore.jks
storePassword=replace-with-secret
keyAlias=upload
keyPassword=replace-with-secret
