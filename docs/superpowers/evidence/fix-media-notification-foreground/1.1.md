任务 1.1 媒体通知与前台服务修复
- 根因A:MediaSession 未 addSession() 注册到 MediaSessionService(无 controller
  连接时 onGetSession 不触发)→ Media3 通知管理器对会话不可见
- 根因B:未声明 POST_NOTIFICATIONS,API 33+ appop=ignore
- 修复后实测(模拟器 API 36):权限弹窗出现并授权;播放后 NotificationRecord
  id=1001 存在,isForeground=true;熄屏 40s 无 freezing、会话 PLAYING;
  锁屏媒体卡片暂停/恢复有效
- verify: ./gradlew :composeApp:check → BUILD SUCCESSFUL
- 日期: 2026-06-10
