任务 0.4 模拟器首屏验证(2026-06-11)
- 环境: DevEco 6.1.1 + 鸿蒙模拟器(aarch64, 127.0.0.1:5557)
- 链路: libshared.so + 头文件部署 ohosApp → Index.ets 默认路由改 WhiteNoiseApp
  → hvigorw assembleHap(debug 无签名)→ hdc install 成功(模拟器豁免签名)
  → aa start 启动 → snapshot_display 截图(0.4-first-screen.jpeg):
  深色底 + 薄荷绿「白噪声 Pro / Kuikly · HarmonyOS 复刻骨架」,即 AppPage.kt
  Compose DSL 渲染结果
- 壳工程定制(快照 biz/ohos-snippets/):
  1) 剥离 demo 专属 bugly 崩溃上报(CrashReport.ets 删除、EntryAbilityStage
     注释、oh-package 去 bugly 依赖)——合规考虑,不引入第三方 SDK
  2) Index.ets 默认 pageName 'router' → 'WhiteNoiseApp'
- 新踩坑: 8) ts-api/provider 由 demo ohosArm64Main 的 knoi @ServiceProvider
  (bugly)生成,剥离 bugly 后无需该文件;9) 模拟器可装未签名 hap,
  真机仍需签名(留给上架前)
