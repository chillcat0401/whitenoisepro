任务 2.2 AVSession + 长时任务后台播放(2026-06-11)
- 实现: AVSession(metadata 标题/artist「白噪屿」+ 播放态 + play/pause/stop
  指令回调直驱实例池);长时任务 AUDIO_PLAYBACK 随播放态启停;
  权限最小化:剥离 demo 的 INTERNET(单机豁免备案根基),声明
  KEEP_BACKGROUND_RUNNING + backgroundModes:["audioPlayback"] + 用途文案
- 实测(hilog + 截图 2.2-control-center.jpeg):
  1) 熄屏(power-shell suspend)60 秒:进程存活、无暂停事件,保活成立
  2) 控制中心媒体卡片正确显示「软雨 · 柔和海浪 / 白噪屿」与播放控制
  3) 卡片暂停→ command:pause → bg task stopped → 双层 paused;
     卡片播放→ command:play → bg task started → 双层 playing(主仓
     fix-notification-resume-card 的坑在鸿蒙侧原生规避)
- 踩坑 13) 长时任务模块名为全小写 @ohos.resourceschedule.backgroundTaskManager
- 应用名定稿「白噪屿」、包名 com.whitenoisepro.hmos(用户确认)
