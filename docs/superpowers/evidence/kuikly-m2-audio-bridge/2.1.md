任务 2.1 ArkTS AudioPlayerModule + 鸿蒙首声(2026-06-11)
- 实现: WNPAudioPlayerModule.ets(AVPlayer 实例池/loop/音量/静音/暂停/停止,
  rawfile fdSrc + 沙箱路径双源),注册 KuiklyViewDelegate;common 侧 AppPage
  挂载模块 + 测试按钮
- 实测(hilog): 双层混音 rain+ocean 各自 initialized→prepared→playing 无错误;
  停止后双层 released;模拟器可听
- 壳定制增量: bundleName→com.whitenoisepro.hmos、应用名→白噪声 Pro、
  两个测试 ogg 进 rawfile
- 新踩坑(均已固化):
  10) libshared.so 与 libshared_api.h 必须同一次构建配套部署——只换 so 不换头,
      cpp 按旧函数表索引新 so,触发 "initKuikly() has not been called" 断言
      cppcrash(已在 build_ohos.sh 改为原子双拷贝)
  11) hvigor 增量构建可能不感知 libs/ 下 so 变化,换 so 后需 hvigorw clean
  12) 换包名后旧包名应用残留前台,极易误读成"新包没生效"——验证前先卸旧包
- 截图: 2.1-test-ui.jpeg
