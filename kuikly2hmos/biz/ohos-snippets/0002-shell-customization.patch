diff --git a/ohosApp/entry/oh-package-lock.json5 b/ohosApp/entry/oh-package-lock.json5
index d0a1f89..2f7c0df 100644
--- a/ohosApp/entry/oh-package-lock.json5
+++ b/ohosApp/entry/oh-package-lock.json5
@@ -7,7 +7,9 @@
   "ATTENTION": "THIS IS AN AUTOGENERATED FILE. DO NOT EDIT THIS FILE DIRECTLY.",
   "specifiers": {
     "@kuikly-open/render@../../core-render-ohos": "@kuikly-open/render@../../core-render-ohos",
+    "@kuiklybase/knoi@0.0.4": "@kuiklybase/knoi@0.0.4",
     "@tencent/libpag@4.4.25": "@tencent/libpag@4.4.25",
+    "libknoi.so@../oh_modules/.ohpm/@kuiklybase+knoi@0.0.4/oh_modules/@kuiklybase/knoi/src/main/types/libknoi": "libknoi.so@../oh_modules/.ohpm/@kuiklybase+knoi@0.0.4/oh_modules/@kuiklybase/knoi/src/main/types/libknoi",
     "libkuikly.so@../../core-render-ohos/src/main/cpp/types": "libkuikly.so@../../core-render-ohos/src/main/cpp/types",
     "libkuikly_entry.so@src/main/cpp/types/kuikly_entry": "libkuikly_entry.so@src/main/cpp/types/kuikly_entry",
     "libpag.so@../oh_modules/.ohpm/@tencent+libpag@4.4.25/oh_modules/@tencent/libpag/src/main/cpp/types/libpag": "libpag.so@../oh_modules/.ohpm/@tencent+libpag@4.4.25/oh_modules/@tencent/libpag/src/main/cpp/types/libpag"
@@ -15,13 +17,23 @@
   "packages": {
     "@kuikly-open/render@../../core-render-ohos": {
       "name": "@kuikly-open/render",
-      "version": "2.4.0",
+      "version": "2.0.0",
       "resolved": "../../core-render-ohos",
       "registryType": "local",
       "dependencies": {
         "libkuikly.so": "file:./src/main/cpp/types"
       }
     },
+    "@kuiklybase/knoi@0.0.4": {
+      "name": "@kuiklybase/knoi",
+      "version": "0.0.4",
+      "integrity": "sha512-8L93bgboLywJE/XTS2sYhrq5NS8mIr9qdv95fiSJXY3nBTLXnsSV37KfUg/CIM7k70z+nmFT44suMMtIl8NISA==",
+      "resolved": "https://ohpm.openharmony.cn/ohpm/@kuiklybase/knoi/-/knoi-0.0.4.har",
+      "registryType": "ohpm",
+      "dependencies": {
+        "libknoi.so": "file:./src/main/types/libknoi"
+      }
+    },
     "@tencent/libpag@4.4.25": {
       "name": "@tencent/libpag",
       "version": "4.4.25",
@@ -32,6 +44,12 @@
         "libpag.so": "file:./src/main/cpp/types/libpag"
       }
     },
+    "libknoi.so@../oh_modules/.ohpm/@kuiklybase+knoi@0.0.4/oh_modules/@kuiklybase/knoi/src/main/types/libknoi": {
+      "name": "libknoi.so",
+      "version": "0.0.0",
+      "resolved": "../oh_modules/.ohpm/@kuiklybase+knoi@0.0.4/oh_modules/@kuiklybase/knoi/src/main/types/libknoi",
+      "registryType": "local"
+    },
     "libkuikly.so@../../core-render-ohos/src/main/cpp/types": {
       "name": "libkuikly.so",
       "version": "2.0.0",
diff --git a/ohosApp/entry/oh-package.json5 b/ohosApp/entry/oh-package.json5
index f8aeb07..731444b 100644
--- a/ohosApp/entry/oh-package.json5
+++ b/ohosApp/entry/oh-package.json5
@@ -9,7 +9,6 @@
     "@kuikly-open/render": "file:../../core-render-ohos",
     "libkuikly_entry.so": "file:./src/main/cpp/types/kuikly_entry",
     "@tencent/libpag": "4.4.25",
-    "bugly": "0.5.1",
     "@kuiklybase/knoi": "0.0.4"
   }
 }
\ No newline at end of file
diff --git a/ohosApp/entry/src/main/ets/entryability/EntryAbilityStage.ets b/ohosApp/entry/src/main/ets/entryability/EntryAbilityStage.ets
index 88763e8..e64a176 100644
--- a/ohosApp/entry/src/main/ets/entryability/EntryAbilityStage.ets
+++ b/ohosApp/entry/src/main/ets/entryability/EntryAbilityStage.ets
@@ -18,7 +18,7 @@ import { AbilityStage } from '@kit.AbilityKit';
 import { hilog } from '@kit.PerformanceAnalysisKit';
 import { setup, init } from '@kuiklybase/knoi';
 import BuildProfile from 'BuildProfile';
-import { CrashReport } from '../kuikly/CrashReport/CrashReport';
+// [whitenoise 壳定制] 剥离 demo 专属 bugly 崩溃上报(合规:不引入第三方 SDK)
 
 export default class EntryAbilityStage extends AbilityStage {
   onCreate(): void {
@@ -27,6 +27,6 @@ export default class EntryAbilityStage extends AbilityStage {
     setup("libshared.so", BuildProfile.DEBUG);
     init();
 
-    CrashReport.getInstance().initCrashReport(this.context);
+    // CrashReport 已剥离
   }
 }
diff --git a/ohosApp/entry/src/main/ets/kuikly/CrashReport/CrashReport.ets b/ohosApp/entry/src/main/ets/kuikly/CrashReport/CrashReport.ets
deleted file mode 100644
index 63972b1..0000000
--- a/ohosApp/entry/src/main/ets/kuikly/CrashReport/CrashReport.ets
+++ /dev/null
@@ -1,65 +0,0 @@
-
-import { BuglyBuilder, Bugly, ICrashListener, TDLog, ModuleName, AppVersionMode } from "bugly"
-import BuildProfile from "BuildProfile";
-import { getKuiklyDemoBuglyService } from "../../ts-api/provider";
-
-const TAG = "BuglyHM";
-
-export class CrashReport {
-  static initialize = false
-  private static _instance: CrashReport | null = null
-  private context: Context | null = null
-
-  private constructor() {}
-
-  static getInstance(): CrashReport {
-    if (!CrashReport._instance) {
-      CrashReport._instance = new CrashReport()
-    }
-    return CrashReport._instance
-  }
-
-  initCrashReport(c: Context): void {
-    if (CrashReport.initialize) {
-      return;
-    }
-    this.context = c
-
-    let builder = new BuglyBuilder();
-
-    builder.appId = 'xxxxxxx';   // 必填，Bugly产品信息中的APP ID
-    builder.appKey = 'xxx-xxxx-xxxx-xxxx-xxxx';    // 必填，Bugly产品信息中的APP KEY    // builder.appId = 'xxxxxxx';   // 必填，Bugly产品信息中的APP ID
-    builder.deviceId = "12345";     // 必填，设备ID，应保证设备ID对不同设备唯一
-    builder.platform = BuglyBuilder.PLATFORM_PRO;    // 必填，设置上报平台，OA版本需设置为[BuglyBuilder.PLATFORM_OA]
-
-    builder.appVersion = '1.0.0';   // 选填，业务的App版本
-    builder.appVersionMode = BuildProfile.DEBUG ?  AppVersionMode.DEBUG : AppVersionMode.RELEASE;//"DEBUG";  // 选填，当前App的版本类型，支持根据不同的版本类型下发配置
-    builder.buildNum = '0';         // 选填，业务App版本的构建号
-    builder.appChannel = 'website'; // 选填，业务App渠道
-    builder.userId = "12345";       // 选填，用户ID，如不设置则为空
-    builder.deviceModel = "huawei"; // 选填，机型，如不设置则为空
-    builder.debugMode = BuildProfile.DEBUG;       // 选填，默认开启，开启后Bugly SDK会打印更多调试日志，线上版本可关闭
-    builder.sdkLogMode = true;      // 选填，设置debugMode或sdkLogMode均可开启Bugly sdk日志，适用于线上关闭debug模式但又希望打印bugly日志的场景
-    builder.initDelay = 0;          // 选填，延迟初始化时间，单位ms
-    builder.enableJsCrashProtect = false;   // 选填，是否开启Js异常崩溃保护，设置为true后发生未捕获Js异常，进程不会退出
-    builder.enablePerfModules = ModuleName.AllModules;  // 选填，开启性能监控项，可传入单个性能模块名称或一组性能模块名称，此处初始化后，还需配置开启对应模块采样率，模块才会真正开启
-
-    Bugly.init(c, builder);   // 如果需等待Bugly完全初始化完成，使用await Bugly.init(context, builder);
-
-    // 通过knoi注册上报回调
-    getKuiklyDemoBuglyService().registerUnhandledExceptionHook(this.postCrashRelatedError);
-    // 注册crash相关的异常上报
-    CrashReport.initialize = true
-  }
-
-  /**
-   * 上报自定义错误
-   */
-  reportError(name: string, errorMsg: string, stack: string): void {
-    Bugly.postCustomError(name, errorMsg, stack, false, this.context)
-  }
-
-  private postCrashRelatedError(name: string, message: string, stack: string, storeFirst: boolean) {
-    Bugly.postCrashRelatedError(name, message, stack)
-  }
-}
diff --git a/ohosApp/entry/src/main/ets/pages/Index.ets b/ohosApp/entry/src/main/ets/pages/Index.ets
index 4ff1ea5..7d406db 100644
--- a/ohosApp/entry/src/main/ets/pages/Index.ets
+++ b/ohosApp/entry/src/main/ets/pages/Index.ets
@@ -77,7 +77,7 @@ struct Index {
     Stack() {
       if (this.showKuikly) {
         Kuikly({
-          pageName: this.pageName ?? 'router',
+          pageName: this.pageName ?? 'WhiteNoiseApp',
           pageData: this.pageData ?? {},
           delegate: this.kuiklyViewDelegate,
           contextCode: this.contextCode,
